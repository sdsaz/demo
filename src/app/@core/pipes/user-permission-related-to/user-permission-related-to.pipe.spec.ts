import { UserPermissionRelatedToPipe } from './user-permission-related-to.pipe';

describe('UserPermissionRelatedToPipe', () => {
  it('create an instance', () => {
    const pipe = new UserPermissionRelatedToPipe();
    expect(pipe).toBeTruthy();
  });
});
