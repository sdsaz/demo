import { ConfiguredEntityNamePipe } from './configured-entity-name.pipe';

describe('ConfiguredEntityNamePipe', () => {
  it('create an instance', () => {
    const pipe = new ConfiguredEntityNamePipe();
    expect(pipe).toBeTruthy();
  });
});
