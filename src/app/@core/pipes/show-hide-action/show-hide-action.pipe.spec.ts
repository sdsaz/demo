import { ShowHideActionPipe } from './show-hide-action.pipe';

describe('ShowHideActionPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowHideActionPipe();
    expect(pipe).toBeTruthy();
  });
});
