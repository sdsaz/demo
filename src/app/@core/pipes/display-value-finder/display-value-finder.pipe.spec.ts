import { DisplayValueFinderPipe } from './display-value-finder.pipe';

describe('DisplayValueFinderPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayValueFinderPipe();
    expect(pipe).toBeTruthy();
  });
});
