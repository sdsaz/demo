import { CommaSeperatorValuePipe } from './comma-seperator-value.pipe';

describe('CommaSeperatorValuePipe', () => {
  it('create an instance', () => {
    const pipe = new CommaSeperatorValuePipe();
    expect(pipe).toBeTruthy();
  });
});
