import { TimeFramePipe } from './time-frame-pipe.pipe';

describe('TimeFramePipePipe', () => {
  it('create an instance', () => {
    const pipe = new TimeFramePipe();
    expect(pipe).toBeTruthy();
  });
});
