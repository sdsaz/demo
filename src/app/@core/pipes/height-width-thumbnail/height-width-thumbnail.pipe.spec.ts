import { HeightWidthThumbnailPipe } from './height-width-thumbnail.pipe';

describe('HeightWidthThumbnailPipe', () => {
  it('create an instance', () => {
    const pipe = new HeightWidthThumbnailPipe();
    expect(pipe).toBeTruthy();
  });
});
