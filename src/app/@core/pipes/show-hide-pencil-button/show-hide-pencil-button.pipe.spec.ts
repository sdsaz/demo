import { ShowHidePencilButtonPipe } from './show-hide-pencil-button.pipe';

describe('ShowHidePencilButtonPipe', () => {
  it('create an instance', () => {
    const pipe = new ShowHidePencilButtonPipe();
    expect(pipe).toBeTruthy();
  });
});
