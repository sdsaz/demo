import { IdGeneratorPipe } from './id-generator.pipe';

describe('IdGeneratorPipe', () => {
  it('create an instance', () => {
    const pipe = new IdGeneratorPipe();
    expect(pipe).toBeTruthy();
  });
});
